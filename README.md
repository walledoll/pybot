# 🌙 СоннИИ Bot — Telegram-бот для анализа снов

> **AI-ассистент в Telegram**, который помогает интерпретировать сны, вести историю и получать персонализированные инсайты — прямо в мессенджере.

---

## 🌟 Возможности

- 📝 **Анализ сна** по текстовому описанию
- 🧠 **AI-толкование** с учётом эмоций и истории снов
- 📚 **История и статистика** по эмоциям (Страх, Радость, Гнев и др.)
- 📝 **Регистрация** — сохранение профиля и данных
- 💎 **Премиум-доступ** — расширенный анализ, без лимитов
- 💳 **Оплата через Робокассу** (тестовый режим)
- 🔐 **Безопасность**: данные хранятся в PostgreSQL

---

## 🛠 Технологии

- **Python 3.10+**
- **Flask** — веб-сервер для webhook
- **pyTelegramBotAPI** — работа с Telegram Bot API
- **PostgreSQL** — надёжное хранение данных (общая БД с бэкендом)
- **Webhook** — мгновенная доставка сообщений (без polling)
- **Робокасса** — приём платежей

---

## 📡 Как работает

1. Пользователь отправляет сон в чат
2. Бот запрашивает эмоцию, связанную со сном
3. На основе описания и эмоции генерируется интерпретация:
   - ✅ **OpenAI API** — если доступен
   - ✅ **Базовый анализ** — резервный режим
4. Сон сохраняется в БД → доступен в `/history` и `/stats`

---

## ⚙️ Локальный запуск

> 💡 Для локального теста рекомендуется использовать `ngrok`.

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Настройте `.env`
```env
TELEGRAM_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=postgresql://user:password@localhost:5432/dreams_db
WEBHOOK_URL=https://ваш-ngrok-url.ngrok.io
```

### 3. Запустите бота
```bash
python bot.py
```

### 4. Пробросьте webhook через ngrok
```bash
ngrok http 10000
```
Затем откройте:
```
https://ваш-ngrok-url.ngrok.io/setwebhook
```

---

## 🌐 Деплой на Render

1. Создайте **Web Service** на [Render](https://render.com)
2. Подключите репозиторий с ботом
3. Укажите:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
4. Добавьте переменные окружения:
   - `TELEGRAM_TOKEN` — ваш токен
   - `DATABASE_URL` — URL PostgreSQL из Render (создайте БД отдельно)
   - `WEBHOOK_URL` — URL вашего сервиса (например, `https://dream-analyzer-bot.onrender.com`)
5. После деплоя откройте:
   ```
   https://ваш-бот.onrender.com/setwebhook
   ```

> ⚠️ **Важно**:  
> - Не используйте SQLite — только **PostgreSQL**  
> - Render перезапускает бесплатные сервисы раз в 15 минут без трафика — это нормально для MVP

---

## 🧩 Команды бота

| Команда       | Описание |
|--------------|--------|
| `/start`     | Приветствие и главное меню |
| `/reg`       | Регистрация (имя, дата рождения, телефон) |
| `/analyze`   | Отправить сон на анализ |
| `/history`   | Посмотреть последние 5 снов |
| `/stats`     | Статистика по эмоциям |
| `/premium`   | Оформить премиум-доступ |

Также доступны кнопки в клавиатуре.

---

## 🔐 Безопасность и ограничения

- Все данные хранятся в защищённой PostgreSQL-базе
- Пароли не используются — только Telegram ID
- Платежи — через **Робокассу** (тестовый режим включён)
- Лимит **3 анализа в день** для бесплатных пользователей

---

## 📄 Лицензия

Проект создан в образовательных целях.  
Не является медицинским или психологическим инструментом.

> 💬 **Помните**: сны — это зеркало подсознания, а не диагноз.  
> СоннИИк — ваш проводник в мир внутренних образов.

---

🌙 *Спи спокойно. Твои сны уже ждут тебя.*